# إعداد Sentry لتطبيق ران RAAN

## 🚨 الوضع الحالي: DSN تجريبي مُعد

✅ **تم إعداد DSN تجريبي**: `https://test-dsn@sentry.io/test-project`
✅ **التطبيق يعمل**: http://localhost:8080/
✅ **Sentry مُفعّل**: يظهر في Console `[Sentry] ✅ تتبع الأخطاء مفعّل`

## 🔄 لتحديث DSN الحقيقي:

### الطريقة السريعة (موصى بها):
```bash
# شغّل هذا الأمر واتبع التعليمات
update-sentry-dsn.bat
```

### الطريقة اليدوية:

#### 1. الحصول على DSN من Sentry
1. اذهب إلى: https://raan-nf.sentry.io/settings/projects/
2. اختر مشروعك (أو أنشئ مشروع جديد إذا لم يكن موجوداً)
3. اذهب إلى **Settings > Client Keys (DSN)**
4. انسخ **DSN** (يبدأ بـ `https://`)

#### 2. تحديث متغيرات البيئة
أضف DSN إلى ملف `.env`:

```bash
VITE_SENTRY_DSN=https://abcd1234@sentry.io/123456789
```

#### 3. اختبار الإعداد
1. شغّل التطبيق محلياً: `npm run dev`
2. اذهب إلى **الإعدادات > التكاملات** في لوحة Admin
3. اضغط زر **"اختبار Sentry"**
4. تحقق من https://raan-nf.sentry.io/issues/ لرؤية الخطأ التجريبي

## 📊 ما يراقبه Sentry

- **أخطاء JavaScript**: جميع الأخطاء غير المعالجة
- **أخطاء React**: أخطاء المكونات والـ lifecycle
- **أداء التطبيق**: تحميل الصفحات والتفاعلات
- **معلومات المستخدم**: ID ودور المستخدم (مشفر)

## 🔧 إعدادات متقدمة

### في لوحة تحكم Admin
يمكنك تحديث DSN من داخل التطبيق:
1. اذهب إلى **الإعدادات > التكاملات**
2. أدخل DSN في حقل "Sentry DSN"
3. احفظ التغييرات

### مراقبة الأخطاء
- اذهب إلى https://raan-nf.sentry.io/issues/
- ستجد جميع الأخطاء مصنفة حسب النوع والتكرار

## ⚠️ ملاحظات مهمة

- **لا ترسل DSN في Git**: تأكد من عدم رفع `.env` لـ Git
- **اختبار محلي**: Sentry لا يرسل أخطاء من localhost
- **الخصوصية**: لا يتم إرسال بيانات حساسة (كلمات مرور، tokens)

---

**تم الإعداد بواسطة**: GitHub Copilot 🤖