# 📁 ملفات التشغيل الجديدة

## 🚀 ملفات Batch للتشغيل السريع

### 1. `run_app_quick.bat` - التشغيل الكامل
```
الاستخدام: انقر نقرة مزدوجة للتشغيل
الوظيفة:
├─ تثبيت المتطلبات تلقائياً
├─ تشغيل التطبيق
├─ فتح المتصفح تلقائياً
└─ عرض روابط الوصول
```

**متى تستخدمه:**
- أول مرة تشغل التطبيق
- عندما تحتاج تثبيت المتطلبات
- للمبتدئين

---

### 2. `start_dev.bat` - التشغيل السريع
```
الاستخدام: انقر نقرة مزدوجة للتشغيل
الوظيفة:
├─ التحقق من وجود المتطلبات
├─ تشغيل التطبيق مباشرة
├─ فتح المتصفح تلقائياً
└─ عرض روابط الوصول
```

**متى تستخدمه:**
- بعد التثبيت الأول
- للمطورين اليومي
- عندما تكون المتطلبات مثبتة

---

### 3. `build_and_run.bat` - وضع الإنتاج
```
الاستخدام: انقر نقرة مزدوجة للتشغيل
الوظيفة:
├─ بناء التطبيق للإنتاج
├─ تشغيل خادم الإنتاج
├─ فتح المتصفح تلقائياً
└─ عرض رابط الإنتاج
```

**متى تستخدمه:**
- لاختبار الإنتاج محلياً
- قبل النشر
- للعرض التوضيحي

---

### 4. `check_setup.bat` - فحص الإعداد ⭐ **جديد**
```
الاستخدام: انقر نقرة مزدوجة للتشغيل
الوظيفة:
├─ فحص وجود ملف .env
├─ التحقق من متغيرات البيئة
├─ فحص تثبيت Node.js و npm
├─ التحقق من وجود node_modules
└─ عرض تقرير مفصل
```

**متى تستخدمه:**
- قبل التشغيل الأول
- عند مواجهة أخطاء
- للتأكد من صحة الإعداد
- **استخدمه دائماً قبل start_dev.bat**

---

## 🌐 روابط الوصول

### وضع التطوير (npm run dev):
- **المحلي:** http://localhost:8080/
- **الشبكة:** http://192.168.1.42:8080/

### وضع الإنتاج (npm run preview):
- **المحلي:** http://localhost:4173/
- **الشبكة:** http://192.168.1.42:4173/

---

## 📱 الوصول من الهاتف

يمكنك الوصول للتطبيق من هاتفك عبر:
1. نفس شبكة WiFi
2. استخدام رابط الشبكة المعروض
3. فتح المتصفح على الهاتف

---

## ⚠️ ملاحظات مهمة

### قبل التشغيل الأول:
1. تأكد من وجود Node.js مثبت
2. تأكد من وجود npm
3. تأكد من ملء ملف `.env` بالقيم الصحيحة

### استكشاف الأخطاء:
- إذا لم يفتح المتصفح: افتح الرابط يدوياً
- إذا ظهر خطأ: تحقق من console المتصفح
- إذا فشل التثبيت: قم بتشغيل `npm install` يدوياً

---

## 🎯 دليل الاستخدام السريع

### للمبتدئين:
```
1. انقر على run_app_quick.bat
2. انتظر التثبيت (دقيقة واحدة)
3. سيفتح المتصفح تلقائياً
```

### للمطورين:
```
1. انقر على start_dev.bat
2. سيبدأ التطبيق فوراً
3. عدل الكود وشاهد التغييرات
```

### للاختبار النهائي:
```
1. انقر على build_and_run.bat
2. انتظر البناء (30 ثانية)
3. اختبر التطبيق كما في الإنتاج
```

---

**🚀 جاهز للتشغيل! اختر الملف المناسب لاحتياجاتك.**