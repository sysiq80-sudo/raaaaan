# 🚀 دليل الاستعداد والإطلاق النهائي لتطبيق ران (RAAN Launch & Release Guide)

> **تاريخ الوثيقة:** 2026-06-03
> **الحالة الحالية:** ✅ جاهز للإطلاق التجريبي المحدود (Pilot) | ❌ غير جاهز للإطلاق العام للجمهور (Public Launch)
> **درجة الجاهزية الحالية:** **89 / 100**

مرحباً بك في لوحة تحكم استعداد إطلاق منصة **ران (RAAN)**. تم إعداد هذه الوثيقة في جذر المشروع لتكون المرجع الرئيسي للإدارة وفريق التطوير لمتابعة المهام المتبقية للوصول بالتطبيق إلى الحالة الإنتاجية الكاملة على المتاجر الرسمية.

---

## 📊 مؤشر الجاهزية والتقييم الفعلي (Readiness Scorecard)

> [!NOTE]
> تم احتساب التقييم بناءً على الفحص البرمجي والمخبري الفعلي لأكواد ومكونات التطبيقات الأربعة و124 كائناً في قاعدة البيانات.

```
+--------------------------------------------------------+
| 🟩 الجاهزية الوظيفية (تطبيقات + ميزات): 35 / 35         |
| 🟩 السلامة والأمان المالي (Watchdog): 22 / 22           |
| 🟩 جودة الكود والهيكلية (0 TS Errors): 14 / 14          |
| 🟩 اكتمال التوثيق الفني (28+ وثيقة): 10 / 10           |
| 🟩 الأدوات التشغيلية والـ Fallback: 9 / 9               |
| 🟨 الخصومات (تغطية الاختبارات 2%): -1 / 0               |
|--------------------------------------------------------|
| 🏆 النتيجة النهائية للجاهزية: 89 / 100                 |
+--------------------------------------------------------+
```

*   **توصية الإطلاق الميداني (Pilot):** ✅ **معتمد.** النظام مؤمن بالكامل وبوابات الدفع الإلكتروني مغلقة لتجنب المشاكل في بيئة التجربة المحدودة.
*   **توصية النشر العام (Play Store):** ❌ **موقوف مؤقتاً** لحين رفع تغطية الاختبارات لـ 10% وتقسيم الملفات المتضخمة.

---

## 🔗 روابط سريعة للوثائق الحيوية (Documentation Index)

للمراجعة التقنية والتشغيلية، يرجى الانتقال إلى الوثائق المتخصصة التالية:

*   🚀 **خطة عمل جاهزية الإطلاق المتبقية:** [LAUNCH_READINESS_AUDIT.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/LAUNCH_READINESS_AUDIT.md)
*   📋 **تقرير التسليم النهائي الشامل:** [FINAL_HANDOFF_REPORT.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/FINAL_HANDOFF_REPORT.md)
*   💸 **تدقيق نظام التسعير والارتباط الجغرافي:** [FARE_AND_GEOGRAPHY_AUDIT.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/FARE_AND_GEOGRAPHY_AUDIT.md)
*   ⚙️ **حالة الميزات الفعلية في الكود:** [ACTUAL_SYSTEM_STATUS.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/ACTUAL_SYSTEM_STATUS.md)
*   🗺️ **استراتيجية الخرائط وإدارة التكلفة:** [MAPS_STRATEGY.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/MAPS_STRATEGY.md)
*   🧭 **فهرس التوثيق العام النشط والمؤرشف:** [DOCUMENTATION_INDEX.md](file:///d:/projects/taksi-iraqi/RAAN/raan/docs/DOCUMENTATION_INDEX.md)

---

## 🛠️ الخطة الموسعة للإطلاق الإنتاجي (9 خطوات عملية)

> [!IMPORTANT]
> **توجيه إداري:** يمنع بناء أي ميزات جديدة حالياً. التركيز ينصب 100% على تأمين البيانات والتشغيل الميداني.

### 1. تأمين مستودع Git وتدوير المفاتيح 🔒
*   **المطلوب:** الاحتراز الأمني وتغيير جميع مفاتيح الربط.
*   **الإجراء:** تغيير مفاتيح الإنتاج لـ Supabase Anon Key ومفاتيح Google Maps API، والتحقق عبر أدوات فحص التسريبات من خلو تاريخ Git من أي نسخ قديمة لملفات الأسرار.

### 2. تدقيق سياسات حماية البيانات (RLS) 🛡️
*   **المطلوب:** تأكدنا من تفعيل RLS لجميع الجداول المخصصة (108 جداول). المطلوب الآن فحص قواعد السياسات لضمان عدم وجود ثغرات منطقية تتيح للمستخدمين رؤية بيانات مستخدمين آخرين.
*   **الإجراء:** محاكاة طلبات مستخدمين بصلاحيات مختلفة والتحقق من حظر الوصول لبيانات الرحلات والمعاملات المالية التي لا تخصهم.

### 3. تقسيم وإعادة هيكلة الملفات المتضخمة ✂️
*   **المطلوب:** معالجة تضخم ملف صفحة الراكب الرئيسية `GoPage.tsx` البالغ حجمه 109KB لتسهيل المراجعة.
*   **الإجراء:** تفكيك الواجهات إلى مكونات منفصلة مثل `PickupLocationSelector` و `FareEstimatePanel` وفصل المسارات في `App.tsx` وتفعيل التحميل الكسول.

### 4. كتابة اختبارات E2E بالـ Playwright 🧪
*   **المطلوب:** سد فجوة اختبارات نهاية لنهاية (العينات البرمجية الحالية فارغة).
*   **الإجراء:** كتابة مسارات الاختبار الأساسية (تسجيل الدخول بالـ OTP، طلب رحلة جديدة، قبول السائق للطلب) وربطها بالـ CI/CD Pipeline.

### 5. تأمين بوابات الدفع الإلكتروني (ZainCash / NASS) 💳
*   **المطلوب:** حل ثغرات شروط السباق (Race Conditions) البرمجية (DR-05 و DR-07) التي تمنع الشحن المزدوج للأرصدة.
*   **الإجراء:** تعديل الدوال الخلفية واستخدام قفل المعاملات الذري (`SELECT FOR UPDATE`) في قاعدة البيانات مع تفعيل جداول معاملات الاسترداد الفريدة (Idempotency Tables).

### 6. إعداد خط الإنتاج لأندرويد (Android Release Pipeline) 📱
*   **المطلوب:** إعداد حزم البناء المتوافقة مع متجر Google Play.
*   **الإجراء:** توليد Keystore مشفرة ومحمية، وتأكيد تفعيل Obfuscation وضغط كود الويب في Gradle، والتحقق من تعطيل تصحيح الويب (WebView Debugging) في بيئة الإنتاج.

### 7. إطلاق ومراقبة الـ Pilot الميداني (أربيل/كسنزان) 🚕
*   **المطلوب:** تشغيل فعلي لبيئة مصغرة ومراقبتها ماليًا وتقنيًا.
*   **الإجراء:** توزيع ملفات APK على 5-10 كباتن و20-50 راكباً، وتشغيل أداة Watchdog كل ساعة، واستعمال طرق الدفع كاش وقسائم فقط.

### 8. فحص تتبع المواقع بالخلفية وتوفير الطاقة 🔋
*   **المطلوب:** التأكد من ثبات تتبع السائق في الخلفية وعدم إغلاق النظام للتطبيق.
*   **الإجراء:** تجربة التتبع الميداني على أجهزة سامسونج وشاومي تحت قيود توفير الطاقة المختلفة وإرشاد السائقين لتعطيل تحسين البطارية.

### 9. الإطلاق العام للجمهور (Public Launch) 🌐
*   **المطلوب:** الانتقال للإنتاج الكامل بعد نجاح الـ Pilot لـ 14 يوماً متواصلة دون أخطاء مالية.
*   **الإجراء:** رفع حزم الـ AAB للمتجر، تفعيل الدفع الإلكتروني المؤمن، وربط خادم التوجيه ونطاق الإنتاج النهائي.

### 10. تدوير الأسرار ونشر الهجرات للإنتاج 🔐
*   **المطلوب:** تأمين الـ Edge Functions وتطبيقات البوت بشكل كامل لمنع أي استغلال خارجي للثغرات بعد تفعيل الـ Webhook.
*   **الإجراء:** تطبيق النسخ الاحتياطي لقاعدة البيانات، تدوير مفتاح الـ `INTERNAL_EDGE_SECRET` الداخلي، تعيين مفتاح الـ `CAPTAIN_TELEGRAM_WEBHOOK_SECRET` السري للبوت لتأمين مسار Telegram Webhook، ثم دفع المايجريشنز المعلقة مباشرة للإنتاج.
*   **ترحيل وتنظيف البيانات:** عملية ترحيل وتنظيف المعاملات التاريخية المكررة في محفظة الراكب مؤتمتة بالكامل داخل ملف المايجريشن v5؛ لا تحتاج لتشغيل أي أوامر SQL يدوية أو القيام بأي ترحيل يدوي للبيانات.

---

## ⚙️ أوامر تشغيلية سريعة للمدير التقني (CTO Console)

### 1. تشغيل أداة التحقق المالي الشاملة (Financial Safety Verification)
لتشغيل فحص نزاهة الحسابات والتأكد من مطابقة الأرصدة 100% وخلوها من التزييف المالي:
```powershell
$env:RAAN_ADMIN_CONFIRM = 'yes' ; $env:RAAN_ENV = 'production' ; npx tsx supabase/verification/run-watchdog-full.ts
```

### 2. بناء ومزامنة تطبيقات الأندرويد للـ Pilot

*   **تطبيق الراكب (Rider App):**
    ```bash
    npm run apk:rider
    ```
*   **تطبيق السائق (Driver App):**
    ```bash
    npm run apk:driver
    ```
*   **تطبيق السيارة (Car App):**
    ```bash
    npm run apk:car
    ```

### 3. توليد وتوقيع حزمة الأندرويد للإنتاج (Keystore & Release Signing)

قبل تجميع التطبيق للإنتاج أو الرفع على متجر Google Play، يجب توليد مفتاح التوقيع وإعداد الخصائص:

1. **توليد ملف الـ Keystore:**
   افتح واجهة الأوامر في المسار `android/app` ونفّذ الأمر التالي:
   ```cmd
   keytool -genkey -v -keystore raan-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias raan-alias
   ```
   *(اختر كلمة مرور قوية واحفظها جيداً، سيقوم الأمر بإنشاء ملف `raan-release-key.jks` بداخل المجلد).*

2. **إعداد ملف `keystore.properties`:**
   انسخ ملف `android/keystore.properties.example` باسم `android/keystore.properties` وضع كلمات المرور فيه:
   ```properties
   storeFile=raan-release-key.jks
   storePassword=كلمة_المرور_الخاصة_بك
   keyAlias=raan-alias
   keyPassword=كلمة_المرور_الخاصة_بك
   ```
   *(ملفات الخصائص والمفاتيح محجوبة تلقائياً في `.gitignore` لضمان أمانها).*

### 4. تدوير أسرار البوت والـ Edge Functions في الإنتاج (Production Security Setup)

قبل الإطلاق الفعلي على الإنتاج، يجب تنفيذ الخطوات الأمنية التالية:

1. **نسخ احتياطي فوري للإنتاج (Production Backup):**
   ```bash
   supabase db dump --project-ref <PRODUCTION_PROJECT_REF> -f prod_backup.sql
   ```

2. **تدوير الرمز السري المشترك لـ Edge Functions:**
   قم بتوليد سر عشوائي قوي وتعيينه في أسرار الإنتاج:
   ```bash
   supabase secrets set INTERNAL_EDGE_SECRET="<رمز_سر_جديد>" --project-ref <PRODUCTION_PROJECT_REF>
   ```
   ثم حدّث الـ Vault في قاعدة بيانات الإنتاج ليتطابق مع الرمز الجديد:
   ```sql
   DO $b$ 
   BEGIN 
     IF EXISTS (SELECT 1 FROM vault.secrets WHERE name = 'internal_edge_secret') THEN 
       UPDATE vault.secrets SET secret = '<رمز_سر_جديد>' WHERE name = 'internal_edge_secret'; 
     ELSE 
       PERFORM vault.create_secret('<رمز_سر_جديد>', 'internal_edge_secret', 'RAAN internal edge function auth token'); 
     END IF; 
   END $b$;
   ```

3. **تعيين مفتاح سر التحقق لبوت تليجرام الكابتن:**
   قم بتعيين الرمز السري في البيئة السحابية للإنتاج:
   ```bash
   supabase secrets set CAPTAIN_TELEGRAM_WEBHOOK_SECRET="<مفتاح_سر_تليجرام_الإنتاج>" --project-ref <PRODUCTION_PROJECT_REF>
   ```
   ثم حدد نفس الـ `secret_token` عبر Telegram Bot API لربطه بالـ Webhook بأمان:
   ```bash
   curl -X POST "https://api.telegram.org/bot<CAPTAIN_BOT_TOKEN>/setWebhook" \
        -H "Content-Type: application/json" \
        -d '{"url": "https://<PRODUCTION_PROJECT_REF>.supabase.co/functions/v1/captain-support-bot", "secret_token": "<مفتاح_سر_تليجرام_الإنتاج>"}'
   ```

4. **تطبيق الهجرات بأمان (دون العبث يدويًا بجدول الـ schema_migrations):**
   نفّذ دفع الهجرات مباشرة إلى قاعدة بيانات الإنتاج:
   ```bash
   supabase db push --project-ref <PRODUCTION_PROJECT_REF>
   ```
   *(تنبيه: عملية ترحيل وتصفية المعاملات المكررة في محفظة الراكب وأرشفتها تتم تلقائياً بالكامل في قاعدة البيانات بمجرد دفع الهجرات، ولا تتطلب أي تدخل يدوي).*

---

## 🗺️ البنية التحتية المستقلة للخرائط والتوجيه (Self-Hosted Geo-Infrastructure)

من أجل ضمان استقلالية التطبيق بنسبة 100% وخفض تكاليف الخرائط والتوجيه إلى الصفر عند التشغيل التجاري الواسع، نوصي باتباع الاستراتيجية التالية قبل الإطلاق الرسمي للجمهور:

### 1. استضافة خادم التوجيه الخاص (Private OSRM Server)
* **التوصية:** خلال فترة الـ Pilot، الخادم العام لـ OSRM كافٍ جداً ولن تواجه مشاكل. ولكن قبل الإطلاق العام للجمهور، نوصي بشدة برفع OSRM على سيرفر خاص بك (VPS) وتغذيته بملف خريطة العراق من موقع Geofabrik.
* **متطلبات السيرفر (العراق فقط):** خادم VPS صغير (1 إلى 2 جيجابايت رام، 1 نواة معالج) بتكلفة $5 إلى $10 شهرياً كافٍ جداً لتحميل وتشغيل ملف خريطة العراق.
* **النتيجة:** سرعة استجابة فائقة جداً في تتبع الكباتن، أمان 100% ضد انقطاع الخدمة، وصفر تكاليف استهلاك لحساب المسارات.

### 2. استضافة خادم العنونة الخاص (Private Nominatim Server)
* **هل يمكن استضافته ذاتياً؟** نعم، Nominatim مفتوح المصدر بالكامل ويمكن استضافته على سيرفر خاص بك تماماً مثل OSRM.
* **متطلبات السيرفر (العراق فقط):** يتطلب Nominatim موارد أعلى من OSRM لأنه يقوم بعمليات بحث ومطابقة نصوص معقدة على قاعدة بيانات PostgreSQL/PostGIS. لاستضافة خريطة العراق فقط، تحتاج خادم VPS بمواصفات متوسطة (أقلها 8 جيجابايت رام، 2 إلى 4 نواة معالج، ومساحة 50 جيجابايت SSD) بتكلفة حوالي $20 إلى $30 شهرياً.
* **البديل الاقتصادي للـ Nominatim:** إذا كنت لا ترغب في إدارة سيرفر Nominatim بنفسك، يمكنك استخدام خدمات سحابية وسيطة ورخيصة جداً مبنية على Nominatim (مثل LocationIQ أو OpenCage) تعطي حدوداً مجانية ضخمة (حوالي 5,000 إلى 10,000 طلب يومياً مجاناً) وبأسعار رمزية جداً مقارنة بجوجل مابس.

---

## ⚠️ التحذيرات الصارمة للمطورين (Golden Rules)

1.  🔴 **لا تعدّل أي Migration قديم أبداً:** أي تعديل في قاعدة البيانات يجب أن يتم عبر إنشاء Migration جديد لتجنب مشاكل التزامن السحابي.
2.  🔴 **الدفع الإلكتروني معطل بقصد للـ Pilot:** لا تقم بتفعيل ZainCash أو NASS قبل حل ثغرات شروط السباق وتجربتها في Watchdog.
3.  🔴 **تريجر التعويض القديم ملغى:** دالة `trigger_driver_compensation_90s` موقوفة ومستبدلة بدرع الكابتن لحماية المحفظة.
4.  🟠 **تعامل بحذر مع GoPage.tsx:** تضخم حجم الملف لـ 109KB يتطلب الحذر عند دمج الأكواد لتجنب كسر الخريطة أو التوجيه.
5.  🟡 **لا تحذف seed.sql:** يحتوي على البيانات الجغرافية والتسعيرية الأساسية للتشغيل.
