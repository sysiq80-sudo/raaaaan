# تطوير الجوال - مرجع إصلاح إشعارات الراكب

هذا الملف يوثق الإصلاحات التي تمت لجعل إشعارات تطبيق جوال الراكب تعمل بشكل مستقر، ويستخدم كمرجع عند أي صيانة مستقبلية.

## الهدف

حل مشكلة عدم وصول إشعارات الراكب على تطبيق الجوال، مع بقاء إشعارات السائق تعمل.

## الأعراض التي كانت موجودة

- إشعارات السائق تصل بشكل طبيعي.
- إشعارات الراكب لا تصل أو تفشل بعد أول محاولة.
- ظهور رسائل في Logs مثل:
  - no subscriptions found for rider
  - FCM error: Requested entity was not found
  - Push failed: 401 Authorization header must be specified

## السبب الجذري

1. اشتراكات الراكب كانت تتكدس أو تصبح منتهية الصلاحية (stale).
2. كان يتم الاعتماد على token قديم من localStorage أحيانا.
3. مسار Web FCM endpoint كان يحصل له fallback غير صحيح إلى POST مباشر على endpoint، وهذا يسبب 401.
4. عند فشل token كان يحدث تنظيف للاشتراك، ثم تصبح الحالة no subscriptions found.

## ما تم تعديله

### 1) توحيد قوالب إشعارات الراكب من قاعدة البيانات

- Migration مفعلة:
  - supabase/migrations/20260801000000_fix_rider_push_uses_auto_settings.sql
- أصبحت إشعارات الراكب تقرأ من notification_auto_settings بدلا من النصوص الثابتة.

### 2) إضافة Trigger لحالة السائق (approved/suspended)

- Migration مفعلة:
  - supabase/migrations/20260801000001_driver_approval_push_trigger.sql
- ربط driver_approved و driver_suspended بمسار فعلي للإرسال.

### 3) تعديل مسار الإرسال في Edge Function

- ملف:
  - supabase/functions/send-push-notification/index.ts

التعديلات الأساسية:

- منع fallback الخاطئ للـ FCM web endpoint عبر POST مباشر.
- إجبار FCM endpoints أن تمر عبر FCM v1 فقط.
- إضافة dedup لاشتراكات Web للراكب عند subscribe.
- تحسين التعامل مع subscription_expired والتنظيف بطريقة أدق.

### 4) تعديل تسجيل Web Push للراكب

- ملف:
  - src/hooks/useRiderWebPushSetup.ts

التعديلات الأساسية:

- تجديد الاشتراك (unsubscribe + subscribe) لتفادي endpoints قديمة.
- منع إعادة استخدام اشتراك stale من المتصفح.

### 5) تعديل تسجيل FCM على جوال الراكب

- ملف:
  - src/hooks/useRiderFCMRegistration.ts

التعديلات الأساسية:

- طلب تسجيل FCM جديد دائما من النظام (بدلا من الاعتماد على token مخزن فقط).
- تنظيف كل fcm:// القديمة للراكب قبل إدراج الاشتراك الجديد.
- إبقاء localStorage كحل احتياطي فقط عند تأخر callback.

## أوامر التنفيذ التي تم استخدامها

- تطبيق migrations:
  - supabase db push

- نشر Edge Function:
  - supabase functions deploy send-push-notification

- بناء Rider:
  - npm run build:rider

- مزامنة Android Rider:
  - npm run cap:sync:rider

- بناء APK Rider:
  - npm run apk:rider

## مسار APK الناتج

- android/app/build/outputs/apk/debug/app-debug.apk

## خطوات الاختبار المعتمدة

1. حذف تطبيق الراكب القديم من الجهاز.
2. تثبيت APK الجديد.
3. تسجيل الدخول بحساب راكب.
4. تنفيذ رحلة وتغيير الحالات:
   - accepted
   - arrived
   - in_progress
   - completed
5. متابعة Logs في send-push-notification والتأكد من:
   - عدم ظهور no subscriptions found for rider بعد الاشتراك
   - عدم ظهور 401 على مسار FCM endpoint fallback القديم
   - وجود إرسال ناجح أو على الأقل بقاء subscription سليمة

## نقاط مهمة لمنع تكرار المشكلة

- أي تعديل على Push في الجوال يجب أن يتبعه:
  - build:rider
  - cap:sync:rider
  - apk:rider
- لا تعتمد على localStorage token وحده كمصدر نهائي.
- حافظ على اشتراك واحد نشط لكل راكب على الجوال.
- لا تستخدم POST مباشر على FCM web endpoint كبديل عن FCM v1.

## حالة النظام بعد الإصلاح

- إشعارات جوال الراكب: تعمل.
- إشعارات السائق: تعمل.
- Firebase secret: مضاف ومفعل.
- مسار الإرسال: مستقر بعد إعادة البناء والمزامنة.
