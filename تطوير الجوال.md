# تطوير الجوال - المرجع الرسمي للتطوير المستقبلي

هذا الملف هو المرجع الرسمي لخطة تطوير تطبيق الجوال في مشروع RAAN، ويغطي القرار المعماري، خارطة التنفيذ، معايير الجاهزية للإنتاج، وملاحق الإصلاحات السابقة.

## الرؤية

بناء تطبيق جوال بمستوى تطبيقات النقل العالمية (مثل Uber/Careem) مع الحفاظ على بنية الباك إند الحالية، وتقليل المخاطر عبر انتقال تدريجي ومدروس.

## القرار المعماري المعتمد (محدَّث — يعكس التنفيذ الفعلي في المستودع)

1. **تطبيقات الجوال الحالية (الإنتاج والتطوير):** **React 18 + Vite + Capacitor 8** مع تطبيقات دخول منفصلة (`RiderApp` / `DriverApp` / `CarApp` / `AdminApp`)، وبناءات منفصلة (`vite.rider.config.ts` وغيرها) ومجلد `android/` واحد مع `scripts/patch-android.bat` لتبديل `applicationId` وموارد FCM بين الراكب والسائق والسيارة.
2. **لوحة التحكم:** تبقى **ويب** (بناء `build:admin`) كما هي، منفصلة عن حزم الراكب/السائق.
3. **الخلفية:** **Supabase** (Postgres + Auth + Realtime + Edge Functions) دون إعادة بناء جذرية.
4. **مسار Expo / React Native:** يُعتبر **خياراً مستقبلياً** (انظر [ملحق B](#ملحق-b-مسار-expo--react-native-اختياري)) وليس المسار المنفَّذ حالياً في هذا المستودع؛ أي مشروع Expo منفصل سيشارك نفس الـ API والمنطق المستخرج من الـ hooks عند اتخاذ القرار لاحقاً.

**سجل القرار:** دمج المرجع الواحد مع الكود الفعلي (كان السابق يذكر Expo كمسار وحيد بينما التنفيذ Capacitor). التوثيق التفصيلي لسير البناء: [docs/BUILD_MATRIX.md](docs/BUILD_MATRIX.md). نشر واجهات الويب المنفصلة (راكب / كابتن / سيارة): [docs/WEB_DEPLOYMENT.md](docs/WEB_DEPLOYMENT.md).

## لماذا هذا القرار مناسب للكود الحالي

1. قابلية إعادة استخدام عالية لمنطق الحالة الحالي:
  - `src/stores/driverStore.ts`
  - `src/stores/riderStore.ts`
2. منطق الأجرة موجود ومتماسك وقابل للنقل:
  - `src/hooks/useFareCalculation.ts`
3. أساس قوي لتدفقات الموقع والسائقين القريبين:
  - `src/hooks/useRiderLocation.ts`
  - `src/hooks/useNearbyDrivers.ts`
4. منظومة وظائف سحابية ناضجة يمكن البناء عليها مباشرة:
  - `supabase/functions/match-ride/index.ts`
  - `supabase/functions/send-push-notification/index.ts`
  - `supabase/functions/complete-ride/index.ts`

## خارطة التنفيذ (12 أسبوع)

> **ملاحظة:** البنود التي تفترض «مشروع Expo» أو ترحيل RN تخص [ملحق (B)](#ملحق-b-مسار-expo--react-native-اختياري). التطوير الجاري على المستودع يعتمد **Capacitor** — راجع [docs/BUILD_MATRIX.md](docs/BUILD_MATRIX.md) لأوامر البناء والمزامنة.

### المرحلة 0: تثبيت النطاق ومؤشرات النجاح (أسبوع 1)

1. تثبيت نطاق MVP: Rider + Driver فقط.
2. تثبيت الرحلات الحرجة: طلب، قبول، وصول، بدء، إكمال، إلغاء، إشعار.
3. اعتماد مؤشرات الأداء الأساسية:
  - زمن وصول الطلب للسائق <= 2 ثانية.
  - نجاح تسليم الإشعارات >= 98%.
  - Crash-Free Sessions >= 99.5%.

### المرحلة 1: تأسيس منصة الجوال (أسبوع 2-3)

1. إنشاء مشروع Expo لتطبيقي السائق والراكب.
2. نقل منطق الدومين إلى طبقة مشتركة (shared domain).
3. تهيئة Supabase Mobile Auth وتخزين الجلسة بطريقة آمنة.
4. إعداد مراقبة الأعطال والأداء (Crash/Latency/Notification metrics).

### المرحلة 2: MVP السائق (أسبوع 4-6)

1. شاشة السائق الرئيسية (Online/Offline + حالة التوفر).
2. استقبال طلبات الرحلات مع قبول/رفض من الإشعار.
3. شاشة الرحلة الحية (Arrived/In Progress/Complete).
4. الإحصائيات الأساسية اليومية.
5. اختبار مكثف على أجهزة Android حقيقية (فئات منخفضة ومتوسطة).

### المرحلة 3: MVP الراكب (أسبوع 7-9)

1. اختيار الانطلاق والوصول وعرض المسار.
2. اختيار نوع المركبة وطريقة الدفع.
3. تتبع الرحلة المباشر مع تحديث الحالة.
4. شاشة إنهاء الرحلة والتقييم.
5. موثوقية إشعارات تغيّر الحالة حتى مع الخلفية.

### المرحلة 4: تقسية المنتج (أسبوع 10-11)

1. تحسين الاعتمادية للشبكات الضعيفة:
  - Retry Queue.
  - Idempotency للعمليات الحرجة.
2. تحسين استهلاك البطارية والبيانات في تتبع الموقع.
3. مراجعة أمنية لتدفقات المصادقة والصلاحيات الحساسة.
4. مراجعة RLS impact على المسارات الحرجة.

### المرحلة 5: الإطلاق التشغيلي (أسبوع 12)

1. Soft Launch على نطاق جغرافي محدود.
2. مراقبة تشغيلية مكثفة لمدة 7 أيام.
3. معالجة أي P0/P1 قبل التوسع.
4. قرار Go/No-Go للتوسع المرحلي.

## تعريف الجاهزية للإنتاج (Go-Live Gates)

1. اكتمال تدفق رحلة السائق والراكب من البداية للنهاية بدون تدخل يدوي.
2. نجاح الإشعارات >= 98% في بيئة تشغيل حقيقية.
3. عدم وجود أعطال حرجة (P0) لمدة 7 أيام متتالية.
4. زمن الوصول لأول شاشة قابلة للتفاعل <= 2.5 ثانية على الأجهزة المتوسطة.
5. دعم عربي RTL كامل واتساق الواجهة عبر Android/iOS.

## نموذج الحوكمة لهذا المرجع

1. هذا الملف هو المرجع الوحيد لقرارات جوال RAAN.
2. أي تغيير معماري جوهري يحتاج إضافة سجل قرار مختصر داخل هذا الملف.
3. تحديث أسبوعي إلزامي يتضمن:
  - المنجزات.
  - المخاطر.
  - الانحرافات عن الخطة.
  - خطة الأسبوع القادم.
4. أي ميزة جديدة خارج نطاق MVP تؤجل إلى ما بعد تحقيق مؤشرات الجاهزية.

## أولويات ما بعد الإطلاق

1. تحسين Offline Mode بشكل أعمق.
2. تحسين خوارزمية المطابقة تدريجياً بالاعتماد على بيانات التشغيل.
3. تحسين استهلاك البطارية لتتبع السائق على مدار اليوم.
4. إضافة A/B Testing لتجربة قبول الرحلات.
5. توسيع أدوات المراقبة التشغيلية والاستجابة للحوادث.

## المخاطر الرئيسية وخطة التخفيف

1. مخاطر الإشعارات بالخلفية:
  - التخفيف: بناء Native Push Pipeline مبكراً وتثبيت اختبارات تسليم إلزامية.
2. توسع النطاق قبل الجاهزية:
  - التخفيف: منع إدخال ميزات غير حرجة قبل Go-Live Gates.
3. تباين أجهزة Android:
  - التخفيف: Device Matrix واختبارات دورية على عدة فئات.
4. دين تقني في مسارات المصادقة:
  - التخفيف: مراجعة أمنية مبكرة لمسارات admin/auth الحساسة.

---

## ملحق (B): مسار Expo / React Native (اختياري)

النقاط التالية تصف **بديلاً معمارياً** إذا قرر الفريق لاحقاً بناء تطبيقين أصليين بدل Capacitor. لا تُنفَّذ تلقائياً مع الكود الحالي.

1. مشروعا **Expo** (راكب + سائق) أو monorepo، مع مشاركة طبقة دومين (منطق أجرة، حالات رحلة، إشعارات) مع الـ backend الحالي.
2. تهيئة Supabase Mobile Auth وتخزين الجلسة بشكل آمن على الجهاز.
3. مراقبة الأعطال والأداء (Crash / Latency / تسليم إشعارات).
4. المراحل الزمنية (أسابيع 2–12) في الأقسام أدناه تبقى **مرجع تخطيط** لأي انتقال لاحق إلى RN؛ عند اعتماد Expo يُحدَّد نطاق MVP ويُنقل إلى جدول تنفيذ منفصل.

---

## ملحق (A): مرجع إصلاح إشعارات الراكب

هذا الملحق يحتفظ بسجل الإصلاحات السابقة كمرجع صيانة، ولا يغيّر القرار المعماري الحالي.

### الهدف

حل مشكلة عدم وصول إشعارات الراكب على تطبيق الجوال، مع بقاء إشعارات السائق تعمل.

### أبرز الأعراض التاريخية

1. إشعارات السائق تصل بشكل طبيعي.
2. إشعارات الراكب لا تصل أو تفشل بعد أول محاولة.
3. ظهور رسائل مثل:
  - `no subscriptions found for rider`
  - `FCM error: Requested entity was not found`
  - `Push failed: 401 Authorization header must be specified`

### السبب الجذري التاريخي

1. اشتراكات راكب stale أو متكدسة.
2. الاعتماد أحياناً على token قديم من localStorage.
3. fallback غير صحيح لمسار Web FCM endpoint.
4. تنظيف الاشتراك بعد فشل token ثم الوصول إلى `no subscriptions found`.

### الإصلاحات التي تم تطبيقها

1. توحيد قوالب إشعارات الراكب من قاعدة البيانات:
  - `supabase/migrations/20260801000000_fix_rider_push_uses_auto_settings.sql`
2. إضافة Trigger لحالة السائق (approved/suspended):
  - `supabase/migrations/20260801000001_driver_approval_push_trigger.sql`
3. تعديل مسار الإرسال في:
  - `supabase/functions/send-push-notification/index.ts`
4. تعديل تسجيل Web Push في:
  - `src/hooks/useRiderWebPushSetup.ts`
5. تعديل تسجيل FCM على الجوال في:
  - `src/hooks/useRiderFCMRegistration.ts`

### أوامر التنفيذ المستخدمة تاريخياً

1. `supabase db push`
2. `supabase functions deploy send-push-notification`
3. `npm run build:rider`
4. `npm run cap:sync:rider`
5. `npm run apk:rider`

### مؤشرات الاستقرار بعد الإصلاح

1. إشعارات جوال الراكب تعمل.
2. إشعارات السائق تعمل.
3. مسار الإرسال مستقر بعد إعادة البناء والمزامنة.

---

آخر تحديث: 2026-03-30

تم الحمد لله رب العالمين
