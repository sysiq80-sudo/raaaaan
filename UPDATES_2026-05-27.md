# 📋 سجل التحديثات — 27 مايو 2026 (خطة الأمان والأداء)

> **حالة البناء:** ✅ TypeScript — 0 أخطاء  
> **حالة Edge Functions:** ✅ match-ride + cron-dispatch — deployed  
> **حالة Migrations:** ✅ system_events — مطبّق على Supabase  

---

## 🎯 ملخص الجلسة

تنفيذ خارطة طريق كاملة للأمان والأداء والمراقبة تضم 11 مرحلة. نتيجة الجلسة: تقليل البث العام على Realtime، تحسين استهلاك GPS والـ polling، إضافة طبقة observability كاملة، وتأمين مفاتيح API.

---

## ✅ المراحل المنجزة

---

### Phase 1 — Security Hardening

**الهدف:** تقليل الأسطح المعرضة للهجوم في قاعدة البيانات وإعدادات Supabase.

**ما تم:**
- مراجعة RLS على الجداول الحساسة
- تأكيد أن `system_events` لديها RLS: admin SELECT فقط
- Edge Functions تستخدم `SUPABASE_SERVICE_ROLE_KEY` عبر Deno.env (لا hardcoded secrets)

---

### Phase 2 — CI Gate

**الهدف:** ضمان أن كل build يمر بفحص TypeScript قبل النشر.

**ما تم:**
- التحقق من أن `npx tsc --noEmit -p tsconfig.app.json` يُشغَّل قبل كل deploy
- نتيجة tsc = 0 أخطاء في نهاية كل مرحلة

---

### Phase 3A — cron-dispatch + match-ride Stateless

**الهدف:** ضمان أن Edge Functions لا تحتفظ بحالة بين الاستدعاءات.

**الملفات:**
- `supabase/functions/match-ride/index.ts`
- `supabase/functions/cron-dispatch/index.ts`

**ما تم:**
- الـ functions تقرأ الإعدادات من `app_settings` في كل استدعاء (لا global state)
- لا متغيرات مشتركة بين الـ requests

---

### Phase 3B — PostGIS Spatial Filter

**الهدف:** استبدال full-table-scan بـ spatial query لجلب السائقين القريبين فقط.

**الملفات:**
- `supabase/functions/match-ride/index.ts`
- `supabase/migrations/20260527400000_drivers_postgis_spatial.sql`

**ما تم:**
- إضافة `get_nearby_drivers(p_lat, p_lng, p_radius_km)` RPC تستخدم `ST_DWithin`
- `match-ride` يستدعي `get_nearby_drivers` بدلاً من جلب كل السائقين
- المهاجرة مطبّقة على Supabase ✅
- `outer_radius = admin_default_radius + priority_bonus + radius_bonus + 10km buffer`

**قبل/بعد:**
| | قبل | بعد |
|---|---|---|
| جلب السائقين | `SELECT * FROM drivers WHERE online=true` | `get_nearby_drivers(lat, lng, radius)` |
| الفلترة الجغرافية | JS بعد جلب الكل | PostgreSQL + PostGIS (ST_DWithin) |

---

### Phase 3C — Targeted Ride Notifications

**الهدف:** منع بث طلبات الرحلات لكل السائقين عبر Realtime — إرسال للمرشحين فقط.

**الملفات:**
- `src/hooks/useDriverNotifications.ts`
- `supabase/functions/match-ride/index.ts`

#### المشكلة السابقة
كل سائق متصل يشترك في `postgres_changes` على `rides` table بـ `filter: 'status=eq.pending'`.  
عند إنشاء رحلة جديدة → Supabase Realtime يُرسل الحدث لـ **كل** N سائق متصل.  
الفلترة (نوع السيارة، المسافة) تحدث client-side بعد الاستلام — تكلفة O(N) على كل رحلة.

#### الحل المنفَّذ

**`useDriverNotifications.ts`:**
- ✅ حذف `postgres_changes INSERT` (كان يُشعر كل السائقين بكل رحلة pending)
- ✅ حذف `postgres_changes UPDATE` (كان بدون filter — يُشعر بكل تحديث على rides)
- ✅ إضافة قناة شخصية `driver-notif-{driverId}` تستمع لـ broadcast event `new_ride_request`
- ✅ الـ channel الموجود الآن فقط لتشغيل catch-up query عند reconnect (تسجيل silent في dedupe)

**`match-ride/index.ts`:**
- ✅ بعد إرسال FCM لـ topDrivers، يُرسل Realtime broadcast عبر REST API لكل مرشح:
  ```
  POST /realtime/v1/api/broadcast
  { messages: [{ topic: "driver-notif-{id}", event: "new_ride_request", payload: {...} }] }
  ```
- ✅ الـ payload يحتوي: `id`, `status: "pending"`, `pickup_location`, `pickup_address`, `dropoff_address`, `estimated_fare`, `vehicle_type`, `prefer_women_driver`, `distance_km`, `eta_minutes`
- ✅ فحص `broadcastRes.ok` مع console.warn عند الخطأ

**التدفق الجديد:**
```
1. راكب ينشئ رحلة (pending)
2. useRideBookingSubmission → يستدعي match-ride فوراً
3. match-ride → PostGIS → N مرشح → FCM + Realtime broadcast لكل مرشح
4. driver-notif-{id} يُفعَّل فقط عند المرشحين ← handleNewRide ← إشعار
5. cron-dispatch يُعيد المحاولة إذا لم يُقبل
```

**المقارنة:**
| | قبل | بعد |
|---|---|---|
| من يستقبل الحدث | كل N سائق متصل | المرشحون فقط (topDrivers) |
| مصدر الإشعار foreground | postgres_changes (global) | broadcast شخصي |
| مصدر الإشعار background | FCM (مُستهدَف) | FCM (مُستهدَف) — بدون تغيير |
| فلترة | client-side بعد استلام الكل | server-side (PostGIS) قبل الإرسال |

---

### Phase 4 — google-maps-proxy

**الهدف:** إخفاء Google Maps API Key عن العميل عبر Edge Function proxy.

**الملف:** `supabase/functions/google-maps-proxy/index.ts`

**ما تم:**
- Edge Function تستقبل طلبات الخرائط من العميل وتُضيف API Key من Deno.env
- المفتاح لا يُرسل للمتصفح مباشرة

---

### Phase 5A — useActiveRide Polling تحسين

**الهدف:** تقليل DB reads للراكب أثناء انتظار السائق.

**الملف:** `src/hooks/useActiveRide.ts`

| | قبل | بعد |
|---|---|---|
| polling interval | 2,000 ms | 5,000 ms |
| DB reads/min | 30 | 12 |

---

### Phase 5B — GPS Sync + LiveRideTracker

**الهدف:** تقليل كتابات GPS وقراءات polling أثناء الرحلة.

#### 5B-1: useDriverLocationSync
**الملف:** `src/hooks/useDriverLocationSync.ts`

| | قبل | بعد |
|---|---|---|
| SYNC_INTERVAL_MS | 5,000 ms | 10,000 ms |
| كتابات driver_live_locations/min | 12 | 6 |

يتوافق مع `MIN_UPDATE_INTERVAL = 10s` الموجودة في DriverHome.

#### 5B-2: LiveRideTracker fallback poll
**الملف:** `src/components/rider/LiveRideTracker.tsx`

| | قبل | بعد |
|---|---|---|
| fallback poll interval | 5,000 ms | 30,000 ms |
| قراءات DB/min | 12 | 2 |

الـ fallback يُستخدم فقط عند انقطاع Realtime — المسار الرئيسي broadcast channel.

---

### Phase 6A — System Events Migration

**الهدف:** جدول مركزي لتسجيل أحداث النظام الحرجة مع تنظيف تلقائي.

**الملف:** `supabase/migrations/20260527500000_system_events.sql`  
**الحالة:** ✅ مطبّق على Supabase (عبر SQL Editor)

**ما تم إنشاؤه:**

```sql
-- الجدول
public.system_events (
  id uuid PRIMARY KEY,
  event_type text NOT NULL,
  severity text CHECK (info|warn|error|critical),
  message text,
  metadata jsonb,
  created_at timestamptz DEFAULT now()
)

-- RLS: السائقون لا يرون / الأدمن يقرأ فقط
-- RPC: log_system_event(p_event_type, p_severity, p_message, p_metadata) SECURITY DEFINER
-- pg_cron: "purge-system-events" — كل 7 أيام يحذف السجلات أقدم من 30 يوماً
```

التحقق: الـ migration أعادت `[{"schedule": 7}]` وهو job ID pg_cron صحيح.

---

### Phase 6B — Edge Functions Instrumentation

**الهدف:** تسجيل الأحداث الحرجة في `system_events` من داخل Edge Functions.

#### cron-dispatch
**الملف:** `supabase/functions/cron-dispatch/index.ts`

| الحدث المُسجَّل | الشرط |
|----------------|-------|
| `cron_batch_all_failed` | كل الرحلات في batch فشلت |
| `cron_batch_partial_failure` | بعض الرحلات فشلت |
| `cron_dispatch_crash` | استثناء غير متوقع |

#### match-ride
**الملف:** `supabase/functions/match-ride/index.ts`

| الحدث المُسجَّل | الشرط |
|----------------|-------|
| `ride_abandoned_no_drivers` | رحلة تُتجاهل بعد `max_retry_rounds` — لا سائقون متصلون |
| `ride_abandoned_no_compatible` | رحلة تُتجاهل — لا سائقون قريبون متوافقون |

---

### Phase 6C — Sentry في useRideBookingSubmission

**الهدف:** التقاط حالات فشل match-ride double-retry في Sentry.

**الملف:** `src/hooks/useRideBookingSubmission.ts`

**ما تم:**
```typescript
import { Sentry } from "@/lib/sentry";

// في catch block لـ double-retry failure:
Sentry.captureException(retryErr, {
  tags: { module: "match-ride", event: "client_retry_failed" },
  extra: { rideId: ride.id }
});
```

---

## 🔒 Hardening لاحق (خارج هذه الخطة)

| البند | الأولوية | الملاحظة |
|-------|----------|----------|
| Realtime Authorization لـ `driver-notif-{id}` | متوسطة | القناة public بالاسم حالياً — أي عميل يعرف UUID يستطيع الاشتراك |
| Supabase Advisor: Security Definer View | منخفضة | تحذير من Supabase Advisor |
| تقييد Google Maps API Key من Cloud Console | عالية | HTTP referrer restrictions + API restrictions |
| مراجعة RLS على `app_settings` | متوسطة | مفتاح الخرائط يُقرأ من العميل إن لم يكن محمياً |
| مراقبة 24 ساعة post-deploy | فورية | DB requests، Edge invocations، Realtime connections |

---

## 📊 تأثير الأداء الإجمالي

| المقياس | قبل | بعد |
|---------|-----|-----|
| سائقون يستقبلون حدث pending ride | كل N سائق متصل | المرشحون فقط (3-5) |
| GPS writes/min أثناء رحلة | 12 | 6 |
| DB reads/min للراكب (fallback) | 12 | 2 |
| DB reads/min للراكب (useActiveRide) | 30 | 12 |
| رحلات يُبحث عنها بـ full-table-scan | 100% | 0% (كلها PostGIS) |
