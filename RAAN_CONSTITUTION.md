# 🧠 ذاكرة مشروع تكسي ران (Raan Taxi - Iraq)
## دستور البرمجة ومنع تكرار الأخطاء

### 1. الهوية والسياق (Context)
- **المشروع:** تطبيق تكسي ذكي يعمل في العراق (الأنبار).
- **الجمهور:** سائقين وركاب عراقيين (اللغة: العربية بلهجة محلية مفهومة).
- **التقنيات:** React, Tailwind CSS, Supabase (DB & Edge Functions), Capacitor (Mobile).

### 2. القواعد الذهبية (Golden Rules) - "ممنوع التجاوز"
- **لا للسكرول (No Scroll):** شاشات الموبايل (خاصة السائق) يجب أن تكون `h-[100dvh]` و `overflow-hidden`. لا تضع `h-screen` أبداً.
- **الهوية البصرية:** استخدام `rounded-3xl` للكروت، وتأثير `glassmorphism` (bg-white/90 + backdrop-blur).
  - **شاشة السائق:** كاردات الرحلة تستخدم خلفية داكنة `bg-slate-900/95` مع `backdrop-blur-md` و `rounded-[2rem]`.
- **الحالة الحقيقية:** حالة السائق تُجلب وتُحدث في جدول `drivers` وليس `profiles`. لا تخلط بينهما.
- **اللغة:** الالتزام بالمصطلحات التي اعتمدناها (مثلاً: "المحفظة" وليس "الرصيد"، "قبول الطلب" وليس "تأكيد").

### 3. قائمة الأخطاء المحظورة (Anti-Patterns)
- **خطأ التحميل اللانهائي:** أي عملية `isLoading` يجب أن تنتهي بـ `finally { setIsLoading(false) }`. لا تترك السائق عالقاً في وضع التحميل.
- **تكرار المنطق:** لا تقم بإعادة بناء نظام الـ GPS أو الـ Heartbeat؛ فهي موجودة ومستقرة. قم بالتحسين فوقها فقط.
- **تداخل الجلسات:** عند التعامل مع دخول/خروج السائق، تأكد دائماً من إيقاف/تشغيل `backgroundLocationService`.
- **أمن البيانات:** لا تضع مفاتيح Supabase أو أي أسرار في ملفات الـ Migrations.

### 4. سجل الدروس المستفادة (Lessons Learned)
- **النبض (Heartbeat):** تم تفعيل نبض كل 30 ثانية لتجنب الجلسات الشبحية. لا تحذف هذا المنطق.
- **المطابقة (Matching):** البحث عن سائق يستمر لـ 10 دقائق من خلال إعادة الطلب (re_match) من جهة العميل كل 60 ثانية.
- **التوافق:** استخدام `keepalive: true` في طلبات الـ fetch عند إغلاق التبويب (`beforeunload`).

### 5. معايير واجهة المستخدم (UI Standards)
- **تجربة السائق:** الأزرار يجب أن تكون ضخمة (على الأقل `h-16`) لسهولة اللمس أثناء القيادة.
- **الكارد المزدوج:** فصل كارد البيانات عن كارد الأزرار في شاشة السائق لزيادة التركيز.
- **التفاعل:** تفعيل الاهتزاز (Vibration) عند وصول طلب جديد أو تغيير حالة الرحلة.
- **ألوان المراحل:**
  - `accepted` (في الطريق): بنفسجي/نيلي 💜
  - `arrived` (انتظار العميل): كهرماني 🟡
  - `in_progress` (الرحلة جارية): أخضر 💚

### 6. كيفية التعامل مع الأوامر الجديدة
1. **افحص أولاً:** قبل كتابة كود جديد، افحص الملفات المتعلقة للتأكد أن الميزة غير موجودة.
2. **التزم بالهيكل:** اتبع نمط `try-catch` الشامل مع عرض `toast` واضح للمستخدم باللغة العربية.
3. **التوافقية:** تأكد أن أي تعديل في واجهة الويب يتوافق مع تطبيق الاندرويد (Capacitor).
