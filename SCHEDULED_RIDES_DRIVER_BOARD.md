-- Driver Scheduled Rides Board (لوحة السائق للرحلات المجدولة)

-- الحاجة لهذا الملف:
-- نعرض للسائق لوحة بالرحلات المجدولة المتاحة له (scheduled)
-- يمكنه الضغط Accept لحجز الرحلة (reserved)
-- لا يمكنه قبول رحلتين متداخلتين في الوقت
-- عند الإلغاء المتأخر (< 1 ساعة)، يتم حظره لمدة ساعة

-- الخطوات في الواجهة:

-- 1) صفحة جديدة: /driver/scheduled-rides
-- 2) عرض:
--    - الرحلات المجدولة (scheduled)
--    - الرحلات المحجوزة من السائق الحالي (reserved + confirmed)

-- 3) في كل رحلة:
--    - المسافة والوقت المتوقع
--    - الأسعار
--    - زر "احجز الآن"
--    - بيانات الراكب (النقيب = عدد التقييمات، التقييم)
--    - ملاحظات

-- 4) عند قبول:
--    - استدعاء accept_scheduled_ride()
--    - تحديث الحالة إلى "reserved"
--    - إظهار ملخص الرحلة

-- 5) في الرحلات المحجوزة:
--    - 45 دقيقة: تذكير بالتأكيد
--    - زر "أؤكد جاهزيتي" → confirm_scheduled_ride()
--    - حالة "confirmed"

-- 6) الإلغاء:
--    - إذا أقل من 60 دقيقة:
--       - استدعاء cancel_scheduled_ride_by_driver()
--       - إرجاع الرحلة للـ pool (driver_id = NULL)
--       - حظر السائق لمدة 1 ساعة

-- 7) في الراكب:
--    - عندما يقبل السائق: "تم حجز سائق لرحلتك ✓"
--    - عندما يؤكد: "السائق مؤكد - وصوله في X دقيقة"

-- ملاحظات تقنية:
-- - استخدام Accept و Confirm لحماية من الإلغاء المتأخر
-- - نطاق البحث يزيد للطلبات العاجلة
-- - High Priority: يوقظ السائقين 10 دقائق قبل الموعد
-- - الحظر المؤقت يحفظ في scheduled_blocked_until
