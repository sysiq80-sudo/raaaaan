-- ══════════════════════════════════════════════════════════════════════════════
-- ران — حذف trigger المزامنة غير المستخدمة لـ driver_locations
--
-- المشكلة: trigger trg_sync_driver_location_to_geospatial يكتب إلى جدول
-- driver_locations عند كل تحديث GPS للسائق (ثانياً بثانية في أوقات الذروة).
-- هذا الجدول غير مستخدم إطلاقاً في الإنتاج — الوظيفة الوحيدة التي تقرأ منه
-- هي في ملف 036_surge_pricing_system.sql الذي لم يُطبّق ويحتوي على أخطاء.
--
-- التأثير: حذف كتابة غير ضرورية عند كل ping موقع → أداء أفضل على Nano tier.
-- ملاحظة: يُحفظ الجدول driver_locations والدالة intact لإعادة الاستخدام
-- عند تطبيق نظام surge pricing في المستقبل.
-- ══════════════════════════════════════════════════════════════════════════════

DROP TRIGGER IF EXISTS trg_sync_driver_location_to_geospatial ON public.drivers;
