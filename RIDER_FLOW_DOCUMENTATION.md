# 🚗 توثيق تدفق رحلة العميل (Rider Flow)

## نظرة عامة
هذا المستند يوثق التدفق الكامل لرحلة العميل من بداية فتح التطبيق حتى إكمال الرحلة والتقييم.

---

## 📋 مراحل رحلة العميل

### 1️⃣ المرحلة الأولى: التحميل والمصادقة
**الملفات**: `RiderHomeCustom.tsx`, `useRiderInitialization.ts`

**التدفق**:
- ✅ تحميل بيانات المستخدم من Supabase
- ✅ التحقق من حالة تسجيل الدخول
- ✅ إذا لم يكن مسجلاً: تحويل إلى صفحة `/auth`
- ✅ إذا كان الملف الشخصي غير مكتمل: عرض `CompleteProfileScreen`

**الحالات**:
- `authLoading`: جاري التحميل → عرض `ScreenSkeleton`
- `!user`: غير مسجل → التحويل إلى صفحة المصادقة
- `!isProfileComplete`: ملف غير مكتمل → عرض شاشة إكمال الملف الشخصي

---

### 2️⃣ المرحلة الثانية: تحديد الموقع الأولي
**الملفات**: `RiderHomeCustom.tsx`, `MapLocationPicker.tsx`

**التدفق**:
- ✅ طلب إذن الوصول للموقع من المتصفح
- ✅ الحصول على الإحداثيات (lat, lng)
- ✅ عكس الترميز الجغرافي (Reverse Geocoding) للحصول على العنوان
- ✅ حفظ الموقع في `pickupCoords` و `pickup`

**الأزرار**:
- 🔘 **زر تحديد الموقع الحالي** (Navigation Icon):
  - الموقع: Header + Map Center
  - الوظيفة: `getCurrentLocation()`
  - الحالة: يعرض spinner أثناء التحميل (`isLocating`)

**السيناريوهات**:
- ✅ النجاح: عرض toast "تم تحديد موقعك" + تعبئة موقع الانطلاق
- ❌ الفشل: عرض toast خطأ
- ⚠️ عدم الدعم: عرض رسالة "المتصفح لا يدعم تحديد الموقع"

---

### 3️⃣ المرحلة الثالثة: اختيار موقع الانطلاق
**الملفات**: `MapLocationPicker.tsx`

**التدفق**:
1. ✅ فتح `MapLocationPicker` في وضع `pickup`
2. ✅ عرض الخريطة مع pin في المركز
3. ✅ المستخدم يسحب الخريطة لتحديد الموقع
4. ✅ البحث عن عنوان باستخدام `LocationSearchInput`
5. ✅ اختيار من الأماكن المحفوظة (Saved Places)
6. ✅ الضغط على "تأكيد موقع الانطلاق 🚗"

**الأزرار**:
- 🔘 **تأكيد موقع الانطلاق**:
  - الوظيفة: `handleConfirm()` → ينادي `onPickupConfirmed()`
  - النتيجة: حفظ الموقع + التحول تلقائياً لوضع dropoff
  - ⚠️ **لا يُغلق** المكون - يبقى مفتوحاً للوجهة

- 🔘 **زر الرجوع** (ArrowRight Icon):
  - في وضع pickup: يغلق المكون
  - في وضع dropoff: يرجع لتعديل موقع الانطلاق

- 🔘 **زر تحديد موقعي** (Navigation Icon):
  - الوظيفة: `centerOnUser()`
  - النتيجة: تحريك الخريطة لموقع المستخدم الحالي

**الحالات**:
- ✅ الموقع داخل منطقة الخدمة: عرض زر تأكيد أخضر
- ⚠️ الموقع خارج منطقة الخدمة: عرض تحذير + المسافة لأقرب منطقة

---

### 4️⃣ المرحلة الرابعة: اختيار الوجهة
**الملفات**: `MapLocationPicker.tsx`

**التدفق**:
1. ✅ بعد تأكيد الانطلاق، `MapLocationPicker` ينتقل تلقائياً لوضع `dropoff`
2. ✅ تغيير اللون من أخضر إلى أزرق
3. ✅ تغيير الأيقونة من Target إلى MapPin
4. ✅ المستخدم يحدد الوجهة بنفس الطريقة
5. ✅ الضغط على "تأكيد الوجهة 🎯"

**الأزرار**:
- 🔘 **تأكيد الوجهة**:
  - الوظيفة: `handleConfirm()` → ينادي `onConfirm()`
  - النتيجة: حفظ الوجهة + **إغلاق** المكون + فتح لوحة الحجز

**المعالجات**:
- `RiderHomeCustom.tsx`:
  - `handleMapPickerConfirm()`: يُستدعى فقط للـ dropoff
  - `handlePickupConfirmedInMap()`: يُستدعى للـ pickup
- `RiderHome.tsx`:
  - نفس المنطق
- `RiderHomeMap.tsx`:
  - نفس المنطق

---

### 5️⃣ المرحلة الخامسة: لوحة الحجز (Booking Panel)
**الملفات**: `RiderHomeCustom.tsx`

**التدفق**:
1. ✅ عرض ملخص الرحلة (من → إلى)
2. ✅ اختيار نوع المركبة (`CompactVehicleSelector`)
3. ✅ إضافة محطات وسيطة اختياري (`MultiStopSelector`)
4. ✅ اختيار رحلة ذهاب وعودة اختياري (`RoundTripSelector`)
5. ✅ اختيار طريقة الدفع (`PaymentMethodBadge`)
6. ✅ عرض تفاصيل السعر (`fareBreakdown`)
7. ✅ الضغط على "احجز الآن"

**الأزرار**:
- 🔘 **احجز الآن**:
  - الوظيفة: `handleBookRide()`
  - التحقق: وجود pickup + dropoff
  - الإجراء: إنشاء ride جديد في قاعدة البيانات
  - النتيجة: الانتقال لشاشة الانتظار

- 🔘 **تعديل** (في الهيدر):
  - الوظيفة: فتح `LocationBottomSheet` لتعديل الوجهة

- 🔘 **رجوع** (ArrowLeft):
  - الوظيفة: `resetBooking()`
  - النتيجة: إعادة ضبط جميع البيانات + العودة لشاشة البحث

**المكونات**:
- ✅ `MultiStopSelector`: إضافة محطات وسيطة
- ✅ `RoundTripSelector`: رحلة ذهاب وعودة
- ✅ `CompactVehicleSelector`: اختيار نوع المركبة
- ✅ `PaymentMethodBadge`: طريقة الدفع

**حساب السعر**:
- Hook: `useFareCalculation`
- المدخلات: pickup + dropoff + vehicle type + distance
- المخرجات: `fareBreakdown` (base_fare, distance_fare, total_fare, etc.)

---

### 6️⃣ المرحلة السادسة: شاشة الانتظار (Waiting Screen)
**الملفات**: `RideWaitingScreen.tsx`

**التدفق**:
1. ✅ عرض رسالة "جاري البحث عن سائق..."
2. ✅ عداد الوقت المنقضي
3. ✅ عدد السائقين القريبين
4. ✅ رسائل تشجيعية دورية
5. ✅ الاستماع لتحديثات الرحلة (Realtime + Broadcast + Polling)
6. ✅ عند قبول السائق: عرض بطاقة السائق

**الأزرار**:
- 🔘 **إلغاء الرحلة**:
  - الوظيفة: فتح `CancellationReasonDialog`
  - النتيجة: إلغاء الرحلة + العودة للشاشة الرئيسية

**الاستماع لتحديثات**:
1. ✅ **Database Realtime**: `postgres_changes` على جدول `rides`
2. ✅ **Broadcast Channel**: رسائل `ride_accepted`
3. ✅ **Polling**: كل 2 ثانية كـ fallback

**عند قبول السائق**:
- ✅ تشغيل صوت احتفالي
- ✅ اهتزاز الجهاز
- ✅ عرض إشعار
- ✅ عرض بطاقة السائق (الاسم، الصورة، التقييم، المركبة، اللوحة)

**الإلغاء التلقائي**:
- ⏱️ إذا لم يُقبل الطلب خلال `maxWaitTimeout` (10-15 دقيقة)
- ✅ إلغاء الرحلة تلقائياً + إشعار المستخدم

---

### 7️⃣ المرحلة السابعة: بطاقة السائق (Driver Card)
**الملفات**: `RideWaitingScreen.tsx`

**التدفق**:
1. ✅ عرض معلومات السائق (الاسم، الصورة، التقييم)
2. ✅ معلومات المركبة (الموديل، اللون، اللوحة)
3. ✅ شريط التتبع المباشر (موقع السائق، الوقت المتبقي)
4. ✅ أزرار الاتصال والواتساب
5. ✅ الضغط على "متابعة" للانتقال للتتبع المباشر

**الأزرار**:
- 🔘 **اتصال** (Phone Icon):
  - الوظيفة: `window.open('tel:...')`
  - النتيجة: فتح تطبيق الهاتف

- 🔘 **واتساب** (MessageCircle Icon):
  - الوظيفة: `window.open('https://wa.me/...')`
  - النتيجة: فتح واتساب

- 🔘 **متابعة التتبع المباشر**:
  - الوظيفة: `handleContinueToTracking()` → `onDriverFound()`
  - النتيجة: الانتقال لشاشة `LiveRideTracker`

**المعلومات المعروضة**:
- ✅ اسم السائق + صورة
- ✅ تقييم السائق (⭐)
- ✅ موديل المركبة + اللون
- ✅ رقم اللوحة (بتنسيق مميز)
- ✅ حالة السائق (متصل، في الطريق، وصل)

---

### 8️⃣ المرحلة الثامنة: التتبع المباشر (Live Tracker)
**الملفات**: `LiveRideTracker.tsx`, `useActiveRide.ts`

**التدفق**:
1. ✅ عرض الخريطة مع موقع السائق والراكب
2. ✅ رسم المسار من موقع السائق إلى موقع الانطلاق
3. ✅ عرض الوقت المتبقي لوصول السائق
4. ✅ إشعارات عند:
   - السائق يقترب (1 كم)
   - السائق وصل
   - بدء الرحلة
   - الاقتراب من الوجهة
   - إكمال الرحلة

**الحالات**:
- `accepted`: السائق في الطريق إليك
- `arrived`: السائق وصل - اخرج الآن!
- `in_progress`: الرحلة جارية
- `completed`: الرحلة انتهت
- `cancelled`: الرحلة ألغيت

**التحديثات التلقائية**:
- ✅ موقع السائق: كل 5 ثوان
- ✅ حالة الرحلة: Realtime subscription
- ✅ Broadcast messages للتحديثات الفورية

**الإشعارات والأصوات**:
- 🔔 صوت + اهتزاز عند كل تغيير في حالة الرحلة
- 📱 إشعار متصفح (Browser Notification)
- 📳 اهتزاز مخصص لكل حدث

---

### 9️⃣ المرحلة التاسعة: إكمال الرحلة
**الملفات**: `LiveRideTracker.tsx`, `RatingDialog.tsx`

**التدفق**:
1. ✅ السائق يصل للوجهة
2. ✅ تحديث حالة الرحلة إلى `completed`
3. ✅ فتح `RatingDialog` تلقائياً
4. ✅ المستخدم يقيّم السائق
5. ✅ عرض ملخص الرحلة (المسافة، الوقت، السعر النهائي)

**نافذة التقييم**:
- ⭐ تقييم من 1 إلى 5 نجوم
- 💬 تعليق اختياري
- 🏷️ وسوم (Tags) سريعة (نظيف، مهذب، سريع، etc.)

**الإجراءات بعد التقييم**:
- ✅ حفظ التقييم في قاعدة البيانات
- ✅ تحديث متوسط تقييم السائق
- ✅ `resetBooking()` لإعادة التطبيق للشاشة الرئيسية

---

## 🔄 إعادة الضبط والتنظيف

### دالة `resetBooking()`
```typescript
const resetBooking = () => {
  setDropoff('');
  setDropoffCoords(null);
  setIntermediateStops([]);
  setTripType('one_way');
  setReturnTime(undefined);
  setRouteDistance(null);
  setRouteDuration(null);
  setShowBookingPanel(false);
  setShowSearchOverlay(true);
  setShowWaitingScreen(false);
  setShowLiveTracker(false);
  setActiveRide(null);
};
```

**متى تُستدعى**:
- ✅ عند الضغط على زر الرجوع في لوحة الحجز
- ✅ عند إلغاء الرحلة
- ✅ بعد إكمال الرحلة والتقييم
- ✅ عند إغلاق شاشة التتبع المباشر

---

## 🎯 الأزرار الرئيسية وربطها

### الهيدر (Header)
| الزر | الأيقونة | الوظيفة | الحالة |
|------|---------|---------|--------|
| القائمة | `Menu` | فتح `RiderSideMenu` | دائماً |
| الإشعارات | `Bell` | عرض الإشعارات | دائماً |
| الدعم | `Headphones` | فتح محادثة الدعم | دائماً |
| الإعدادات | `Settings` | فتح `CustomizationPanel` | دائماً |
| الموقع الحالي | `Navigation` | تحديد الموقع | عند الحاجة |

### MapLocationPicker
| الزر | الأيقونة | الوظيفة | الملاحظات |
|------|---------|---------|-----------|
| رجوع | `ArrowRight` | إغلاق / العودة لـ pickup | حسب الوضع |
| موقعي | `Navigation` | تمركز الخريطة | دائماً |
| تأكيد الانطلاق | `Check` + 🚗 | حفظ pickup + الانتقال لـ dropoff | وضع pickup |
| تأكيد الوجهة | `Check` + 🎯 | حفظ dropoff + فتح لوحة الحجز | وضع dropoff |

### لوحة الحجز (Booking Panel)
| الزر | الوظيفة | الحالة |
|------|---------|--------|
| رجوع | `resetBooking()` | دائماً |
| تعديل | فتح `LocationBottomSheet` | دائماً |
| احجز الآن | `handleBookRide()` | disabled أثناء الحجز |

### شاشة الانتظار (Waiting Screen)
| الزر | الوظيفة | الحالة |
|------|---------|--------|
| إلغاء الرحلة | فتح نافذة اختيار السبب | قبل القبول |
| اتصال | الاتصال بالسائق | بعد القبول |
| واتساب | فتح محادثة واتساب | بعد القبول |
| متابعة | الانتقال للتتبع المباشر | بعد القبول |

---

## 📊 Hooks المستخدمة

### 1. `useRiderInitialization`
- تحميل بيانات المستخدم والجلسة
- التحقق من اكتمال الملف الشخصي
- إدارة حالة المصادقة

### 2. `useFareCalculation`
- حساب سعر الرحلة
- المدخلات: pickup, dropoff, vehicle type, distance
- المخرجات: fareBreakdown

### 3. `useOptimizedNearbyDrivers`
- جلب السائقين القريبين
- التحديث التلقائي كل ثانية
- تصفية حسب نوع المركبة

### 4. `useActiveRide`
- تتبع الرحلة النشطة
- الاستماع للتحديثات
- إدارة الحالات (waiting, tracking, completed)

### 5. `useRiderLocation`
- تحديث موقع الراكب في قاعدة البيانات
- تفعيل عند تسجيل الدخول فقط

---

## ✅ التحققات والتحقق من الصحة

### قبل الحجز
- ✅ المستخدم مسجل دخول
- ✅ الملف الشخصي مكتمل
- ✅ موقع الانطلاق محدد
- ✅ الوجهة محددة
- ✅ الموقع داخل منطقة الخدمة (اختياري - تحذير فقط)

### أثناء الحجز
- ✅ إنشاء سجل الرحلة في قاعدة البيانات
- ✅ استدعاء دالة `match-ride` لإيجاد سائق
- ✅ الاستماع لقبول السائق

### أثناء الرحلة
- ✅ تحديث موقع السائق كل 5 ثوان
- ✅ إعادة رسم المسار عند تغيير الموقع
- ✅ إشعارات عند الأحداث المهمة

---

## 🚨 معالجة الأخطاء

### أخطاء الموقع
- ❌ المتصفح لا يدعم تحديد الموقع
- ❌ المستخدم رفض إذن الموقع
- ❌ timeout في الحصول على الموقع
- ✅ عرض رسالة خطأ واضحة + اقتراح الحلول

### أخطاء الحجز
- ❌ لا يوجد سائقون متاحون
- ❌ خطأ في قاعدة البيانات
- ❌ انقطاع الاتصال بالإنترنت
- ✅ عرض toast مع تفاصيل الخطأ

### أخطاء الدفع
- ❌ رصيد المحفظة غير كافٍ
- ❌ فشل عملية الدفع
- ✅ عرض رسالة + خيار طريقة دفع بديلة

---

## 🎨 تجربة المستخدم (UX)

### الحركات والانتقالات
- ✅ `framer-motion` لجميع الانتقالات
- ✅ مدة قياسية: 200-300ms
- ✅ easing: `ease-in-out`

### التغذية الراجعة
- ✅ Toast notifications لجميع الإجراءات
- ✅ Loading states واضحة
- ✅ تعطيل الأزرار أثناء المعالجة
- ✅ رسائل خطأ مفهومة بالعربية

### إمكانية الوصول
- ✅ `aria-label` لجميع الأزرار
- ✅ حجم نقر كافٍ (44x44px على الأقل)
- ✅ تباين ألوان واضح

---

## 🔔 الإشعارات والأصوات

### أنواع الإشعارات
1. ✅ **Toast**: رسائل داخل التطبيق
2. ✅ **Browser Notification**: إشعارات المتصفح
3. ✅ **Sound**: أصوات تنبيه
4. ✅ **Vibration**: اهتزاز الجهاز

### الأحداث
| الحدث | الصوت | الاهتزاز | الإشعار |
|-------|-------|----------|---------|
| قبول السائق | ✅ | ✅ | ✅ |
| السائق يقترب | ✅ | ✅ | ✅ |
| السائق وصل | ✅ | ✅✅ | ✅ |
| بدء الرحلة | ✅ | ✅ | ✅ |
| إكمال الرحلة | ✅ | ✅ | ✅ |

---

## 📱 الاستجابة (Responsive)

### نقاط التوقف (Breakpoints)
- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

### التكيف
- ✅ أحجام خطوط مختلفة
- ✅ تباعد مناسب لكل شاشة
- ✅ إخفاء/إظهار عناصر حسب المساحة

---

## 🔐 الأمان والخصوصية

### بيانات الموقع
- ✅ طلب إذن صريح من المستخدم
- ✅ عدم تخزين الموقع إلا عند الحاجة
- ✅ تشفير البيانات المرسلة

### بيانات الدفع
- ✅ عدم تخزين معلومات البطاقة
- ✅ استخدام بوابات دفع آمنة
- ✅ تأكيد الدفع قبل الإكمال

---

## 🐛 التصحيح والسجلات

### Console Logs
- ✅ استخدام prefixes واضحة: `[RideWaiting]`, `[MapPicker]`
- ✅ emojis للوضوح: 🔍, ✅, ❌, 📡
- ✅ تفاصيل كافية لتتبع المشاكل

### أمثلة
```javascript
console.log('🎯 MapPicker dropoff confirmed:', location);
console.log('✅ Driver found via DB subscription!');
console.log('❌ Booking error:', error);
```

---

## 📝 ملاحظات مهمة

1. ⚠️ **MapLocationPicker**: لا يُغلق بعد تأكيد الـ pickup - يبقى مفتوحاً للانتقال لوضع dropoff تلقائياً
2. ⚠️ **onConfirm vs onPickupConfirmed**: `onConfirm` يُستدعى فقط للـ dropoff، و `onPickupConfirmed` للـ pickup
3. ⚠️ **mapPickerMode**: لا يتحدث داخل `MapLocationPicker` - يجب عدم الاعتماد عليه في المعالجات
4. ⚠️ **Realtime subscriptions**: استخدام 3 طرق للتأكد من التحديثات (DB realtime + Broadcast + Polling)

---

## 🎯 خلاصة التدفق

```
1. تحميل التطبيق
   ↓
2. التحقق من المصادقة
   ↓
3. تحديد موقع الانطلاق (MapLocationPicker - pickup mode)
   ↓
4. تأكيد الانطلاق → التحول التلقائي لوضع الوجهة
   ↓
5. تحديد الوجهة (MapLocationPicker - dropoff mode)
   ↓
6. تأكيد الوجهة → فتح لوحة الحجز
   ↓
7. اختيار تفاصيل الرحلة (مركبة، دفع، محطات)
   ↓
8. الضغط على "احجز الآن"
   ↓
9. شاشة الانتظار (البحث عن سائق)
   ↓
10. قبول السائق → عرض بطاقة السائق
    ↓
11. متابعة → التتبع المباشر للرحلة
    ↓
12. إكمال الرحلة → التقييم
    ↓
13. resetBooking() → العودة للشاشة الرئيسية
```

---

## ✅ قائمة التحقق النهائية

- [x] جميع الأزرار مربوطة بالدوال الصحيحة
- [x] التدفق من pickup إلى dropoff يعمل بسلاسة
- [x] لا توجد فجوات في تدفق العميل
- [x] معالجة الأخطاء موجودة في كل مرحلة
- [x] الإشعارات والأصوات تعمل
- [x] التحققات والصلاحيات موجودة
- [x] الواجهة responsive على جميع الأحجام
- [x] التوثيق شامل وواضح

---

**آخر تحديث**: 9 يناير 2026
**الإصدار**: 1.0
**الحالة**: ✅ مكتمل وجاهز للإنتاج
